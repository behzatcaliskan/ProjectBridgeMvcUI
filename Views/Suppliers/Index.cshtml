@model IEnumerable<projectbridgemvc.Models.Supplier>

@{
    ViewData["Title"] = "Suppliers";
}

<h2 class="mt-4">Tedarikçiler</h2>

<a asp-action="Create" class="btn btn-success mb-3">Yeni Tedarikçi Ekle</a>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Tedarikçi Adı</th>
            <th>Telefon</th>
            <th>Şehir</th>
            <th>İşlemler</th>
        </tr>
    </thead>

    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.SupplierId</td>
                    <td>@item.SupplierName</td>
                    <td>@item.Phone</td>
                    <td>@item.City</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.SupplierId" class="btn btn-primary btn-sm">Güncelle</a>
                        <a asp-action="Delete" asp-route-id="@item.SupplierId" class="btn btn-danger btn-sm">Sil</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center">Kayıt bulunamadı.</td>
            </tr>
        }
    </tbody>
</table>